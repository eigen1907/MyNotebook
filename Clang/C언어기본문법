C89 표준
    ANSI에 의해 채택된 첫 번째 표준
    ANSI C라고도 부름
    대부분의 컴파일러가 지원하는 표준
    오늘날 대부분의 C코드도 이 버전
        많은 임베디드 시스템은 여전히 C89만 지원
        특화된 소형 하드웨어는 전용 운영체제를 지원
        그 하드웨어 + 운영체제 용으로 컴파일이 가능, 하드웨어에 직접 접근
    따라서 이 과목의 절반 이상을 C89 표준에 할애

Hello World
#include
    C#에서 using 지시문과 비슷한 일을 함
    다른 파일에 구현된 함수나 변수를 사용할 수 있게 해줌
    다만 C#처럼 알아서 함수나 변수를 찾아주지는 않음
    전처리기 지시문 중 하나!
    전처리기(preprocessor)란?
        컴파일을 하기 전에 텍스트를 복붙해주는 일을 함
        나중에 봄...
#include<>
    <>안에 단어(stdio.h)는 실제 디스크 상에 존재하는 파일 이름
    헤더파일(header)을 발견하면?
    -> 해당 파일을 열어 내용 복사!
    즉 저 문구 대신 모든 텍스트가 맨위에 붙게 됨
    자세한건 컴파일/링크에서 배움
    다른 방법으로는 <> 대신 ""를 사용 가능 근데 일단 쓰지 말 것
        나중에 뭔지 배움

stdio.h
    C표준 라이브러리 중 일부
    libc에서 표준 입출력을 담당
    스트림 입출력에 관련된 함수들을 포함
    C#의 System네임스페이스와 비슷한 역할을 가짐
    stdio라이브러리안에는?
        printf()
        scanf()
        fopen()
        fclose() 등등...

C표준 라이브러리란?
    다음에 필요한 매크로, 자료형(data type), 함수 등을 모아 놓은 것
        문자열 처리
        수학 계산
        입출력 처리
        메모리 관리

main(void)함수
int main(void)
{
    return 0;
}
    프로그램의 진입점(entry point)
    C코드를 빌드해서 나온 실행파일(.exe또는 .out)을 실행하면 main(void)함수가 자동적으로 실행됨

이름과 반환형
    반드시 int를 반환해야 함
        0:프로그램에 문제가 없었다는 뜻
    